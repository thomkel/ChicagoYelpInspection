<%= form_for(@inspection) do |f| %>
  <% if @inspection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@inspection.errors.count, "error") %> prohibited this inspection from being saved:</h2>

      <ul>
      <% @inspection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inspect_id %><br>
    <%= f.number_field :inspect_id %>
  </div>
  <div class="field">
    <%= f.label :inspect_date %><br>
    <%= f.date_select :inspect_date %>
  </div>
  <div class="field">
    <%= f.label :results %><br>
    <%= f.text_field :results %>
  </div>
  <div class="field">
    <%= f.label :violations %><br>
    <%= f.text_area :violations %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
